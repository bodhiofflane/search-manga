import {Box} from '@mui/material';

const Footer = () => {
    return (
        <Box
            component={'footer'}
            sx={{mt: 'auto'}}
        >
        </Box>
    );
};

export default Footer;
